// Client SDK — reserved for future real-time features (e.g. live post preview).
// All current data fetching uses the Admin SDK via server components and API routes.
//
// To enable: npm install firebase
// Then initialise here with NEXT_PUBLIC_ env vars.
export {}

